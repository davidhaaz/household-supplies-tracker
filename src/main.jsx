import React from "react";
import ReactDOM from "react-dom/client";
import HouseholdTracker from "../household-supplies-tracker.jsx";

ReactDOM.createRoot(document.getElementById("root")).render(
  <React.StrictMode>
    <HouseholdTracker />
  </React.StrictMode>,
);
